name: remoteconfig

on:
  pull_request:
    paths:
    - 'FirebaseRemoteConfig**'
    - '.github/workflows/remoteconfig.yml'
    - 'scripts/**'
    # Rebuild on Ruby infrastructure changes.
    - 'Gemfile'
  schedule:
    # Run every day at 11pm (PST) - cron uses UTC times
    - cron:  '0 7 * * *'

jobs:
#  remoteconfig:
#    env:
#      plist_secret: ${{ secrets.GHASecretsGPGPassphrase1 }}
#    runs-on: macos-latest
#    steps:
#    - uses: actions/checkout@v2
#    - name: Setup Bundler
#      run: scripts/setup_bundler.sh
#    - name: Install Secret GoogleService-Info.plist
#      run: scripts/decrypt_gha_secret.sh scripts/gha-encrypted/storage-db-plist.gpg \
#          FirebaseStorage/Tests/Integration/Resources/GoogleService-Info.plist "$plist_secret"
#    - name: BuildAndTest # can be replaced with pod lib lint with CocoaPods 1.10
#      run: ([ -z $plist_secret ] || scripts/third_party/travis/retry.sh scripts/build.sh FirebaseRemoteConfig all)

  catalyst:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup Bundler
      run: scripts/setup_bundler.sh
    - name: Setup project and Build for Catalyst
      # Only build the unit tests on Catalyst. Test stopped working when GHA moved to Xcode 11.4.1.
      run: scripts/test_catalyst.sh FirebaseRemoteConfig build FirebaseRemoteConfig-Unit-unit

#  quickstart:
#    env:
#      plist_secret: ${{ secrets.GHASecretsGPGPassphrase1 }}
#      signin_secret: ${{ secrets.GHASecretsGPGPassphrase1 }}
#    runs-on: macOS-latest
#    steps:
#    - uses: actions/checkout@v2
#    - name: Setup quickstart
#      run: scripts/setup_quickstart.sh remoteconfig
#    - name: Install Secret GoogleService-Info.plist
#      run: scripts/decrypt_gha_secret.sh scripts/gha-encrypted/qs-storage.plist.gpg \
#          quickstart-ios/storage/GoogleService-Info.plist "$plist_secret"
#    - name: Install Secret FIREGSignInInfo.h
#      run: scripts/decrypt_gha_secret.sh scripts/gha-encrypted/FIREGSignInInfo.h.gpg \
#          quickstart-ios/TestUtils/FIREGSignInInfo.h "$signin_secret"
#    - name: Test quickstart
#      run: ([ -z $plist_secret ] || scripts/third_party/travis/retry.sh scripts/test_quickstart.sh RemoteConfig)

  pod-lib-lint:
    runs-on: macOS-latest

    strategy:
      matrix:
        target: [ios, tvos, macos]
    steps:
    - uses: actions/checkout@v2
    - name: Setup Bundler
      run: scripts/setup_bundler.sh
    - name: Build and test
      run: scripts/third_party/travis/retry.sh scripts/pod_lib_lint.rb FirebaseRemoteConfig.podspec --platforms=${{ matrix.target }}

  remoteconfig-cron-only:
    runs-on: macos-latest
    if: github.event_name == 'schedule'
    strategy:
      matrix:
        target: [ios, tvos, macos]
        flags: [
          '--use-modular-headers',
          '--use-libraries'
        ]
    needs: pod-lib-lint
    steps:
    - uses: actions/checkout@v2
    - name: Setup Bundler
      run: scripts/setup_bundler.sh
    - name: PodLibLint Storage Cron
      run: |
        scripts/third_party/travis/retry.sh scripts/pod_lib_lint.rb FirebaseRemoteConfig.podspec --platforms=${{ matrix.target }} ${{ matrix.flags }}
        scripts/third_party/travis/retry.sh scripts/pod_lib_lint.rb FirebaseRemoteConfig.podspec --platforms=${{ matrix.target }} ${{ matrix.flags }}
